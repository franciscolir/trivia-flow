rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Portadas: cualquier persona puede verlas;
    // solo usuarios autenticados pueden subir/borrar archivos.
    match /covers/{fileName} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
